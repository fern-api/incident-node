// This file was auto-generated by Fern from our API Definition.

import type * as IncidentIO from "../index.js";

export interface EscalationPathNodeLevelV2 {
    /** Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks */
    ack_mode?: EscalationPathNodeLevelV2.AckMode;
    round_robin_config?: IncidentIO.EscalationPathRoundRobinConfigV2;
    /** The targets (users or schedules) for this level */
    targets: IncidentIO.EscalationPathTargetV2[];
    /** If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive */
    time_to_ack_interval_condition?: EscalationPathNodeLevelV2.TimeToAckIntervalCondition;
    /** How long should we wait for this level to acknowledge before proceeding to the next node in the path? */
    time_to_ack_seconds?: number;
    /** If the time to ack is relative to a time window, this identifies which window it is relative to */
    time_to_ack_weekday_interval_config_id?: string;
}

export namespace EscalationPathNodeLevelV2 {
    /** Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks */
    export const AckMode = {
        All: "all",
        First: "first",
    } as const;
    export type AckMode = (typeof AckMode)[keyof typeof AckMode];
    /** If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive */
    export const TimeToAckIntervalCondition = {
        Active: "active",
        Inactive: "inactive",
    } as const;
    export type TimeToAckIntervalCondition =
        (typeof TimeToAckIntervalCondition)[keyof typeof TimeToAckIntervalCondition];
}
